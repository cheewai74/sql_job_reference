E.g:</BR>
ALTER TABLE "Book" RENAME TO book;</BR>

Note:</BR>
When populating or creating table name or column name in pgadmin, it has to be in small cap else table name has double quote. This is a known PGADMIN issue.</BR>
