source .venv/bin/activate</BR>

pip install 'vanna[postgres]'</BR>
python3 -m pip install --upgrad pip</BR>
sudo apt install jupyter-notebook</BR>

pip freeze > requirements.txt
pip install -r requirements.txt

